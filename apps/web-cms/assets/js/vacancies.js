(function () {
  "use strict";

  // The original Next.js vacancies page has no page-specific client logic.
  // This file is intentionally kept small so CMS can attach future behaviour here
  // without modifying the shared assets/js/main.js.
})();
